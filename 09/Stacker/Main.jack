/** Initializes a new Stacker game and starts running it. */
class Main {
    function void main() {
        var StackerGame game;
        let game = StackerGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}